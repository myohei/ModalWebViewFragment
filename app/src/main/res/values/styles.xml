<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.ModalWebView" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/modal_webview_bg</item>
        <item name="android:windowEnterAnimation">@anim/modal_enter</item>
        <item name="android:windowExitAnimation">@anim/modal_exit</item>
    </style>

</resources>
